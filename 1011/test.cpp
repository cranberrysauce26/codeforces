#include <bits/stdc++.h>
#include <cstdio>

int doit(int x) {
    if (x == 0) {
        exit(0);
    }
    assert(x == 1);
    printf("yo\n");
    return 0;
}

int main() {
    doit(0);
    printf("ho\n");
}