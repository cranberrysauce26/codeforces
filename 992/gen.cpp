#include <bits/stdc++.h>
using namespace std;

int main() {
    freopen("data.txt", "w", stdout);
    int n = 2e5, k = 1;
    printf("%d %d\n", n, k);
    // printf("1\n%d\n", )
    for (int i=1; i <= n; ++i) {
        printf("%d\n", rand() + 1);
    }
    // ab / (a+b)
}